export function pgrest_schemaless_set (path, value, priority)
  # 用json-patch修改json
  # 設定priority (path_p: priority)
  ...

export function pgrest_schemaless_get (path)
  # use json-pointer to get json
  # sort with priorities keys
  ...
